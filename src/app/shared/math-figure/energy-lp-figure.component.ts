import { Component } from '@angular/core';

/**
 * Typeset LP formulation for the energy-auction battery optimizer.
 *
 * Rendered as markup rather than shipped as a screenshot so the equations stay
 * sharp at any zoom, follow the active theme, and remain selectable text.
 */
@Component({
  selector: 'app-energy-lp-figure',
  standalone: true,
  imports: [],
  templateUrl: './energy-lp-figure.component.html',
  styleUrl: './energy-lp-figure.component.scss',
})
export class EnergyLpFigureComponent {}
